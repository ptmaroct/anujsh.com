[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "22"

# Trailing-slash normalization is handled by Netlify's default Pretty URLs.
# Custom redirect rules were removed because `force = true` caused a redirect
# loop on /blog/ (Netlify strips the trailing slash before matching the rule).
